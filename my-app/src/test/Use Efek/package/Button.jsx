export const Button = ({ click }) => {
  return (
    <div>
      <button onClick={click}>Tambah nilai</button>
    </div>
  );
};

export const Button2 = ({ click2 }) => {
  return (
    <div>
      <button onClick={click2}>Kurangi nilai</button>
    </div>
  );
};
